import React from "react";
import { Container } from "react-bootstrap";

function About() {
    return (
        <Container className="mt-4">
            <h2>About My Photo App</h2>
            <p>
                This application allows you to upload, view, and explore images in a user-friendly gallery.
                Navigate through the sections to discover features.
            </p>
        </Container>
    );
}

export default About;
